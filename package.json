{
  "name": "ai-news-for-ciso",
  "version": "1.0.0",
  "private": true,
  "description": "AI News for CISO — weekly newsletter site and editorial pipeline",
  "type": "module",
  "scripts": {
    "build": "node src/build.js",
    "serve": "node src/build.js && npx serve dist",
    "send": "node src/send.js",
    "deploy": "bash scripts/deploy.sh"
  },
  "dependencies": {
    "marked": "^15.0.0"
  }
}
